using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleGenerator : MonoBehaviour
{


    // public ObjectPooler ObstaclePool;               // Pool of coins
    public ObstaclePool ObstaclePool;
      public float distanceBetweenObstacles;

   /*   public void SpawnCoins(Vector3 startPosition)
      {
        GameObject obstacle1 = ObstaclePool.GetPooledObject(); // add coin at start position
          obstacle1.transform.position = startPosition;
         obstacle1.SetActive(true);

      /*    GameObject coin2 = ObstaclePool.GetPooledObject();
          coin2.transform.position = new Vector3(startPosition.x - distanceBetweenCoins, startPosition.y, startPosition.z);       // add coin to left of start position
          coin2.SetActive(true);

          GameObject coin3 = ObstaclePool.GetPooledObject();
          coin3.transform.position = new Vector3(startPosition.x + distanceBetweenCoins, startPosition.y, startPosition.z);       // add coin to right of tart positon
          coin3.SetActive(true);
    */
    //   }

    


    


}
